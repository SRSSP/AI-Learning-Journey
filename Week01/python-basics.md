
# Data Types in Python
int, float, str.

# Variables
We can directly define variables in python 
age = 25
salary = 300000
is_active = True

# Conditions
if(condition):
  print()
else:

if(condition):
  print()
elif():
print()
else:
print()

# Loops
# for 
  for variable in collection:
   print(variable)

# functions
A function is a block of code that performs a specific task. Instead of writing the same code multiple times, you write it once and call it whenever you need it.
ex: def greet(name):
        print(f"Hello {name}")
calling the function
greet("Praveen")

